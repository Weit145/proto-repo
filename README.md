## 📦 Proto Files

Каталог **proto** содержит все файлы **Protocol Buffers (`.proto`)**, используемые в микросервисной платформе форума.

В данных файлах описаны gRPC-сервисы, структуры сообщений и контракты взаимодействия между микросервисами.
Proto-файлы являются **единым источником истины** для межсервисных API и обеспечивают строгую типизацию, совместимость и независимое развитие сервисов.

Этот репозиторий подключен как сабмодуль ко всем бэкенд-сервисам проекта **[Microservices_Site](https://github.com/Weit145/microservices_site)**.

---

### 📂 Структура

Файлы организованы по сервисам, за которые они отвечают:

| Файл                    | Описание                                                              |
| ----------------------- | --------------------------------------------------------------------- |
| `admin/admin.proto`     | Определяет `Admin` сервис для административных задач.                 |
| `auth/auth.proto`       | Определяет `Auth` сервис для аутентификации и авторизации.            |
| `post/post.proto`       | Определяет `Post` сервис для управления постами.                       |
| `user/user.proto`       | Определяет `User` сервис для управления профилями пользователей.      |
| `user_post/user_post.proto` | Определяет `UserPost` сервис для связи пользователей и постов. |

---

### 🔧 Назначение

- Описание gRPC-сервисов и их методов.
- Определение сообщений и структур данных, используемых для запросов и ответов.
- Формирование строгих контрактов взаимодействия между микросервисами.

---

### 🧩 Использование

- Применяется всеми gRPC-сервисами платформы.
- Используется для кодогенерации:
  - Серверных реализаций (stubs) на стороне сервисов-провайдеров.
  - Клиентских SDK на стороне сервисов-потребителей.
- Обеспечивает согласованность API и строгую типизацию данных между сервисами.
- Позволяет сервисам развиваться независимо при сохранении обратной совместимости.
