syntax = "proto3";
package admin;

option go_package = "github.com/Weit145/proto-repo/admin;admin";


message DeletePostRequest{
    int32 post_id = 1;
}

message DeleteUserRequest{
    int32 user_id = 1;
}

message BanUserRequest{
    int32 user_id = 1;
}

message Empty{}

service Admin{
    rpc DeletePost(DeletePostRequest) returns (Empty);
    rpc DeleteUser(DeleteUserRequest) returns (Empty);
    rpc BanUser(BanUserRequest) returns (Empty);
}