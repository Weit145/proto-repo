## 📦 Proto Files

Каталог **proto** содержит все файлы **Protocol Buffers (`.proto`)**, используемые в микросервисной платформе форума.

В данных файлах описаны gRPC-сервисы, структуры сообщений и контракты взаимодействия между микросервисами.  
Proto-файлы являются **единым источником истины** для межсервисных API и обеспечивают строгую типизацию, совместимость и независимое развитие сервисов.

Каталог `proto` используется всеми сервисами проекта **[Microservices_Site](https://github.com/Weit145/microservices_site)**.


### 🔧 Назначение

- описание gRPC-сервисов и их методов;
- определение сообщений и структур данных;
- формирование контрактов взаимодействия между микросервисами.

---

### 🧩 Использование

- применяется всеми gRPC-сервисами платформы;
- используется для генерации:
  - серверных gRPC-интерфейсов;
  - клиентских SDK;
- обеспечивает согласованность API и строгую типизацию данных между сервисами;
- позволяет сервисам развиваться независимо при сохранении совместимости.

---

Если хочешь, следующим шагом могу:
- описать **структуру proto-файлов** (common, auth, user и т.д.);
- добавить **versioning и backward compatibility**;
- или привести этот раздел к формату production-документации.